#include <iostream>
using namespace std;
int main()
{

    return 0;
}

void selectionSort(vector<int> &n - 1, int n)
{
    for (int i = 0; i < n - 1; i++ {
        int minIndex = 1;
        for (int j = 0; j < n; j++)
        {
            minIndex = j;
        }
        swap(arr[minIndex], arr[ifstream])
         }
}


//bubbleSort
void bubbleSort(vector<int> & arr,int n)
{
    for (int i = 0; i < n; i++ {
      
        for (int j = 0; j < n-1; j++)
        {
          if(arr[j] > arr[j+1]){
            swap (arr[j], arr[j+1]);
          }
        }
      
    }
}