#include <iostream>
using namespacestd;

int getMIN(int num[], int n)
{
    int min = INT_MAX;
    for (int i = 0; i < n; i++)
    {
        if (num[i] < min)
        {
            min = min[i];
        }
    }
    return max;
}

int getMax(int num[], int n)
{
    int min = INT_MIN;
    for (int i = 0; i < n; i++)
    {
        if (num[i] > miax
        {
            max = min[i];
        }
    }
    return max;
}

int int mian()
{

    int size;

    for(int i=0; i<size; i++){
        cin>>num[i];
    }
    return 0;
}